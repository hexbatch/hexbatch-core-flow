
<ul class="list-group list-group-flush flow-tags-list" data-project_guid="{{ project.get_project_guid() }}">
    {% for tag in project.get_all_owned_tags_in_project(true) %}
    <li class="list-group-item">
        <div class="row">
            <div class="col-12 col-xl-6 flow-tag-show-edit-on-click" data-tag_guid="{{ tag.getGuid() }}">
                <span class="flow-tag-list-name flow-tag-display  flow-tag-{{ project.get_project_guid() }}"
                      data-tag_guid="{{ tag.getGuid() }}"
                      style="display: none"
                >
                    {{ tag.getName() }}
                </span>
            </div> <!-- /.col -->

            <div class="col-12 col-xl-6">
                <ul class="list-group">
                    {% for applied in tag.getApplied() %}
                        <a href="{{applied.getZTaggedUrl}}" class="list-group-item list-group-item-action ">
                            <span class="flow-tag-applied-target-title" data-applied_guid="{{ applied.getGuid() }}">
                            {% include 'tag/partials/applied/applied-title.twig' with {'applied': applied} %}
                            </span>
                        </a>
                    {% endfor %}
                </ul>
            </div> <!-- /.col -->
        </div> <!-- /.row -->
    </li> <!-- /.list-group-item -->
    {% endfor %}
</ul> <!-- /.list-group -->



<script>


    jQuery(function(){

        update_tags_in_display();

    });




</script>