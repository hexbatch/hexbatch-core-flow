<nav aria-label="breadcrumb">
    <ol class="breadcrumb">

        <li class="breadcrumb-item">
            <a href="{{  url_for('all_projects')  }}">{{ project.get_admin_user().flow_user_name }}</a>
        </li>

        <li class="breadcrumb-item">
            <a href="{{ url_for('single_project_home',{
                user_name: project.get_admin_user().flow_user_name ,
                project_name: project.flow_project_title
            } )
            }}">
                <span class="flow-project-title">
                   Editing {{ project.flow_project_title }}
                </span>
            </a>
        </li>

        <li class="breadcrumb-item active" aria-current="page">Editing {{ project.flow_project_title }}</li>
    </ol>
</nav>

<div class="row">
    <div class="col-12 col-lg-auto ">
        {{ include('project/partials/edit-side-menu.twig') }}
    </div><!-- /.col -->

    <div class="col ">
        {{ include('project/partials/project-form.twig') }}
        <div class="w-100 mt-2"></div>
        {% include 'tag/partials/applied-control.twig' with{
            'tags_of_applied': project.get_applied_tags(),
            'applied_target_guid':project.flow_project_guid,
            'applied_target_type': 'project'
        } %}
    </div><!-- /.col -->

</div> <!-- /.row -->

{{ include('tag/partials/tag-js-support.twig') }}
