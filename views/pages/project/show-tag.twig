

<div class="container p-0 m-1">

    <div class="card col-12 col-lg-9">

        <div class="card-header">
            Tag <i>{{ tag.flow_tag_name}}</i> For Project {{ project.flow_project_title }}
        </div>

        <div class="card-body">
            {{ include('tag/partials/show-tag.twig', {'tag': tag}) }}
        </div>

    </div>
</div>

{{ include('tag/partials/tag-js-support.twig') }}



